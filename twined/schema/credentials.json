{
    "type": "array",
    "items": [
        {
            "type": "object",
            "properties": {
                "name": {
                    "type": "string"
                },
                "default": {
                    "type": "string"
                },
                "location": {
                    "type": "string",
                    "pattern": "^local|google$",
                    "description": "Location of environment variable containing the credential."
                },
                "project_name": {
                    "type": "string",
                    "description": "Name of Google Cloud project that credential exists in (only required if location field is 'google')."
                },
                "version": {
                    "type": "string",
                    "description": "Version of Google Cloud secret to use for credential (only required if location field is 'google')."
                }
            },
            "required": ["name"]
        }
    ]
}
